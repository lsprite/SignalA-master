<resources>

    <!-- PopupAnimation -->
    <!-- 自定义loading dialog -->
    <style name="loading_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/loading_bg</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    <!-- SmoothProgressBar -->

    <style name="GPlusProgressBar" parent="SmoothProgressBar">
        <item name="spb_stroke_separator_length">5dp</item>//线条之间的间隔
        <item name="spb_sections_count">5</item>//屏幕中的线条数量
        <item name="spb_speed">0.7</item>//Bar的行进速度
        <item name="spb_interpolator">spb_interpolator_linear</item>//Bar的行进动画
        <item name="spb_colors">@array/gplus_colors</item>//Bar的颜色数组
    </style>
</resources>